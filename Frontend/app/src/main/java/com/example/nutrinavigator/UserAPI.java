/*package com.example.nutrinavigator;

import androidx.appcompat.app.AppCompatActivity;

import java.util.List;


public interface UserAPI {
        // Fetch the list of followed users
        @GET("/social/listfollowed")
        Call<List<User>> getFollowingList();

        // Unfollow a user
        @POST("/social/unfollow")
        Call<Void> unfollowUser(@Query("username") String username);

        // Follow a user
        @POST("/social/follow")
        Call<Void> followUser(@Query("username") String username);

        // Fetch the list of followers
        @GET("/social/listfollowers")
        Call<List<User>> getFollowersList();
    }

}
*/
